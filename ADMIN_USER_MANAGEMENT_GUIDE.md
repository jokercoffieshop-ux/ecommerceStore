# دليل إدارة المستخدمين للمسؤول
# Admin User Management Guide

## نظرة عامة | Overview

تم إنشاء نظام إدارة مستخدمين كامل يسمح للمسؤولين بعرض وتعديل وحذف حسابات المستخدمين.

A complete user management system has been created that allows admins to view, update, and delete user accounts.

---

## الميزات | Features

### 1. عرض جميع المستخدمين | View All Users
- عرض قائمة كاملة بجميع المستخدمين مع معلوماتهم الأساسية
- عرض صورة الملف الشخصي لكل مستخدم
- إظهار عدد الطلبات لكل مستخدم
- عرض حالة التحقق (موثق/غير موثق)
- عرض دور المستخدم (عميل/موظف/مدير)

### 2. البحث والتصفية | Search & Filter
- البحث بالاسم أو البريد الإلكتروني أو رقم الهاتف
- تصفية المستخدمين حسب الدور (الكل/عملاء/موظفين/مدراء)
- صفحات متعددة (Pagination) مع إمكانية التحكم في عدد النتائج

### 3. عرض التفاصيل | View Details
- عرض معلومات المستخدم الكاملة
- إحصائيات المستخدم (عدد الطلبات، رموز التحقق)
- عرض آخر 5 طلبات للمستخدم
- تواريخ التسجيل والتحديث

### 4. تعديل المستخدم | Edit User
- تعديل الاسم والبريد الإلكتروني والهاتف
- تغيير دور المستخدم (عميل/موظف/مدير)
- تغيير حالة التحقق
- إعادة تعيين كلمة المرور (اختياري)
- التحقق من البيانات قبل الحفظ

### 5. حذف المستخدم | Delete User
- حذف حساب المستخدم بشكل دائم
- تحذير إذا كان للمستخدم طلبات
- تأكيد الحذف قبل التنفيذ
- حماية: لا يمكن للمدير حذف حسابه الخاص

---

## البنية التقنية | Technical Structure

### Backend APIs

#### 1. GET /api/admin/users
**الوصف:** الحصول على قائمة المستخدمين مع التصفية والبحث

**المعاملات (Query Parameters):**
- `page` - رقم الصفحة (افتراضي: 1)
- `limit` - عدد النتائج في الصفحة (افتراضي: 10)
- `role` - تصفية حسب الدور (CLIENT, STAFF, ADMIN, ALL)
- `search` - البحث في الاسم/البريد/الهاتف

**الاستجابة:**
```json
{
  "users": [
    {
      "id": 1,
      "name": "أحمد محمد",
      "email": "ahmed@example.com",
      "phone": "+201012345678",
      "role": "CLIENT",
      "isVerified": true,
      "avatarUrl": "/uploads/avatars/...",
      "createdAt": "2025-01-01T00:00:00.000Z",
      "updatedAt": "2025-01-01T00:00:00.000Z",
      "_count": {
        "orders": 5
      }
    }
  ],
  "pagination": {
    "total": 50,
    "page": 1,
    "limit": 10,
    "totalPages": 5
  }
}
```

#### 2. GET /api/admin/users/[id]
**الوصف:** الحصول على تفاصيل مستخدم محدد

**الاستجابة:**
```json
{
  "user": {
    "id": 1,
    "name": "أحمد محمد",
    "email": "ahmed@example.com",
    "phone": "+201012345678",
    "role": "CLIENT",
    "isVerified": true,
    "avatarUrl": "/uploads/avatars/...",
    "createdAt": "2025-01-01T00:00:00.000Z",
    "updatedAt": "2025-01-01T00:00:00.000Z",
    "_count": {
      "orders": 5,
      "otps": 2
    },
    "orders": [
      {
        "id": 1,
        "orderNumber": "ORD-001",
        "status": "DELIVERED",
        "totalAmount": "250.00",
        "createdAt": "2025-01-01T00:00:00.000Z"
      }
    ]
  }
}
```

#### 3. PUT /api/admin/users/[id]
**الوصف:** تحديث بيانات مستخدم

**البيانات المطلوبة (Body):**
```json
{
  "name": "أحمد محمد المحدث",
  "email": "ahmed.new@example.com",
  "phone": "+201098765432",
  "role": "STAFF",
  "isVerified": true,
  "password": "NewPassword123" // اختياري
}
```

**الاستجابة:**
```json
{
  "message": "تم تحديث بيانات المستخدم بنجاح",
  "user": {
    "id": 1,
    "name": "أحمد محمد المحدث",
    "email": "ahmed.new@example.com",
    ...
  }
}
```

#### 4. DELETE /api/admin/users/[id]
**الوصف:** حذف مستخدم

**المعاملات (Query Parameters):**
- `force` - حذف قسري حتى لو كان للمستخدم طلبات (true/false)

**الاستجابة:**
```json
{
  "message": "تم حذف المستخدم بنجاح",
  "userId": 1
}
```

**في حالة وجود طلبات:**
```json
{
  "message": "المستخدم لديه 5 طلب. هل تريد حذف الحساب رغم ذلك؟",
  "hasOrders": true,
  "ordersCount": 5
}
```

---

## Frontend Pages

### صفحة إدارة المستخدمين
**المسار:** `/admin/users`

**المكونات:**
1. **رأس الصفحة** - عنوان ووصف
2. **شريط البحث والتصفية** - بحث وتصفية حسب الدور
3. **جدول المستخدمين** - عرض جميع المستخدمين
4. **أزرار الإجراءات** - عرض/تعديل/حذف
5. **أشرطة الصفحات** - التنقل بين الصفحات

**النوافذ المنبثقة (Modals):**
1. **نافذة التفاصيل** - عرض معلومات المستخدم الكاملة
2. **نافذة التعديل** - تعديل بيانات المستخدم
3. **نافذة الحذف** - تأكيد حذف المستخدم

---

## الأمان | Security

### 1. التحقق من الصلاحيات
- يجب أن يكون المستخدم مسجل دخول
- يجب أن يكون دور المستخدم "ADMIN"
- التحقق من التوكن في كل طلب

### 2. الحماية
- لا يمكن للمدير تعديل حسابه الخاص
- لا يمكن للمدير حذف حسابه الخاص
- التحقق من البريد الإلكتروني قبل التحديث (عدم التكرار)
- تشفير كلمة المرور عند التحديث

### 3. التحقق من البيانات
- استخدام Zod للتحقق من البيانات
- التحقق من صيغة البريد الإلكتروني
- التحقق من صيغة رقم الهاتف المصري
- التحقق من قوة كلمة المرور

---

## كيفية الاستخدام | How to Use

### 1. الوصول إلى صفحة إدارة المستخدمين
1. سجل الدخول كمدير
2. انتقل إلى لوحة التحكم `/admin/dashboard`
3. اضغط على "إدارة المستخدمين" أو انتقل مباشرة إلى `/admin/users`

### 2. البحث عن مستخدم
1. اكتب في شريط البحث (الاسم/البريد/الهاتف)
2. اضغط على زر البحث 🔍
3. أو استخدم التصفية حسب الدور

### 3. عرض تفاصيل مستخدم
1. اضغط على زر العين 👁️ في صف المستخدم
2. ستظهر نافذة بكل تفاصيل المستخدم
3. اضغط "إغلاق" للعودة

### 4. تعديل مستخدم
1. اضغط على زر القلم ✏️ في صف المستخدم
2. عدّل البيانات المطلوبة
3. اضغط "حفظ التغييرات"

### 5. حذف مستخدم
1. اضغط على زر سلة المهملات 🗑️ في صف المستخدم
2. أكد الحذف في النافذة المنبثقة
3. إذا كان للمستخدم طلبات، ستظهر رسالة تحذير

---

## ملاحظات هامة | Important Notes

### الأدوار (Roles)
- **CLIENT** (عميل) - المستخدم العادي الذي يقوم بالطلبات
- **STAFF** (موظف) - يمكنه إدارة الطلبات والمنتجات
- **ADMIN** (مدير) - صلاحيات كاملة على النظام

### حالة التحقق (Verification)
- **موثق** - تم التحقق من البريد الإلكتروني
- **غير موثق** - لم يتم التحقق بعد

### القيود (Restrictions)
- لا يمكن للمدير تعديل أو حذف حسابه الخاص
- المستخدمون الذين لديهم طلبات يحتاجون تأكيد إضافي للحذف
- البريد الإلكتروني يجب أن يكون فريداً

---

## استكشاف الأخطاء | Troubleshooting

### 1. خطأ "غير مصرح"
- تأكد من تسجيل الدخول كمدير
- تحقق من صلاحياتك

### 2. فشل التحديث
- تحقق من صحة البيانات المدخلة
- تأكد من عدم تكرار البريد الإلكتروني
- تحقق من صيغة رقم الهاتف

### 3. فشل الحذف
- تحقق من عدم محاولة حذف حسابك الخاص
- إذا كان للمستخدم طلبات، يجب التأكيد على الحذف

---

## الملفات المضافة | Added Files

### Backend
- `pages/api/admin/users/index.js` - API للحصول على قائمة المستخدمين
- `pages/api/admin/users/[id].js` - API لعرض/تعديل/حذف مستخدم

### Frontend
- `pages/admin/users.js` - صفحة إدارة المستخدمين

### Styles
- تم تحديث `styles/Admin.module.css` - إضافة أنماط إدارة المستخدمين

---

## API Testing Examples

### 1. Get All Users
```bash
curl -X GET "http://localhost:3000/api/admin/users?page=1&limit=10&role=ALL" \
  -H "Cookie: token=YOUR_TOKEN"
```

### 2. Get User Details
```bash
curl -X GET "http://localhost:3000/api/admin/users/1" \
  -H "Cookie: token=YOUR_TOKEN"
```

### 3. Update User
```bash
curl -X PUT "http://localhost:3000/api/admin/users/1" \
  -H "Content-Type: application/json" \
  -H "Cookie: token=YOUR_TOKEN" \
  -d '{
    "name": "Updated Name",
    "role": "STAFF",
    "isVerified": true
  }'
```

### 4. Delete User
```bash
curl -X DELETE "http://localhost:3000/api/admin/users/1" \
  -H "Cookie: token=YOUR_TOKEN"
```

### 5. Force Delete User with Orders
```bash
curl -X DELETE "http://localhost:3000/api/admin/users/1?force=true" \
  -H "Cookie: token=YOUR_TOKEN"
```

---

## الخلاصة | Summary

تم إنشاء نظام إدارة مستخدمين متكامل يوفر:
- ✅ عرض جميع المستخدمين مع التصفية والبحث
- ✅ عرض تفاصيل كاملة لكل مستخدم
- ✅ تعديل بيانات المستخدمين (الاسم، البريد، الهاتف، الدور، التحقق، كلمة المرور)
- ✅ حذف المستخدمين مع الحماية والتحذيرات
- ✅ واجهة مستخدم عربية سهلة الاستخدام
- ✅ أمان كامل مع التحقق من الصلاحيات
- ✅ التحقق من البيانات قبل الحفظ
- ✅ Pagination للتعامل مع عدد كبير من المستخدمين

النظام جاهز للاستخدام ويمكن الوصول إليه من `/admin/users`
